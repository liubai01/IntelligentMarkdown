# Markdown Rendering Fixtures

Category for generic Markdown rendering validation fixtures.

Included members:

- `markdown_features.config.md`
- `links.config.md` + `link_target.md` — link navigation (relative, external, anchor)

